# 配置与安全边界

本仓库只交付工具代码、合成测试和使用说明。账号、数据权限和运行数据属于使用者。

- 不提交内网服务地址、公司代理、专用域名、账户标识、持仓、数据库、缓存或真实日志。
- API Key 通过使用者自己的环境变量配置；代码只保留变量名称，不携带任何有效值。
- OpenD 默认连接本机，且仅在显式选择 futu provider 时启用。该通用本机默认值不指向作者环境。
- 代理设置由使用者控制；工具不预置作者代理，也不清除使用者的代理设置。
- 本版未集成遥测、自动上传、交易调用或作者托管的数据服务。
- 发布时只导出审核过的文件，并检查源码、安装包、压缩包与实际 Git 提交。
  原项目历史、本地验证记录和环境文件不属于发布内容。

请只向仓库维护者提交不含敏感信息的最小复现。若发现密钥，先撤销或轮换该密钥，
不要把密钥粘贴进 issue、聊天、截图或提交历史。

源码脱敏不替代第三方依赖安全审计，也不授予下载数据的再分发权限。
本机离线测试、公开网络样本和接收方实际环境验收的范围见 VERIFICATION.md。
